/**
 *  This file serves as a central hub for importing and exporting SVG icons used throughout the application
 * Icons are imported as React components using the ?react query parameter
 * The file includes common UI icons like:
 * - Navigation icons (arrows, chevrons)
 * - Action icons (plus, close, download)
 * - Status icons (check, alert, error)
 * - Object icons (folder, file, box)
 * - User interface icons (eye, copy, dots)
 * All icons are exported as named exports for use in other components
 * 
 * Eg:
 * import { ReactComponent as PlusIcon } from "./plus.svg?react";
 * import { ReactComponent as CloseIcon } from "./close.svg?react";
 * ... rest of imports

 * export {
 *   PlusIcon,
 *   CloseIcon,
 *   ... rest of exports
 * };
 */

export {};
