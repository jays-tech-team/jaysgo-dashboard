export enum TOOLTIP_MESSAGES {
  GLOBAL_VALUE = "The value is global for all language",
}

export enum AnalyticsDateType {
  ORDER_DATE = "order_date",
  PAYMENT_DATE = "payment_date",
}

export enum ORDER_CHANNEL {
  ALL = "all",
  WEB = "web",
  MOBILE = "mobile",
  BACKOFFICE = "backoffice",
}
